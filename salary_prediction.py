# -*- coding: utf-8 -*-
"""Salary_Prediction

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1-YNGD1b0koceWpcyuwd6Ql_ty0M4s7kZ

# Salary Prediction on the basis of given experiance using simple linear regression.

## Importing the libraries
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

"""## Importing the dataset"""

dataset = pd.read_csv('Salary_Data.csv')
X = dataset.iloc[:, :-1].values
y = dataset.iloc[:, -1].values

"""## Splitting the dataset into the Training set and Test set"""

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X,y, test_size = 0.2, random_state=0)

"""## Training the Simple Linear Regression model on the Training set"""

from sklearn.linear_model import LinearRegression
regressor = LinearRegression()
regressor.fit(X_train, y_train)

"""## Predicting the Test set results"""

y_pred = regressor.predict(X_test)

"""## Visualising the Training set results"""

plt.scatter(X_train, y_train, color="red")
plt.plot(X_train, regressor.predict(X_train), color="blue")
plt.title("Salary vs Experiance (Training Set)")
plt.xlabel("Years of Experiance")
plt.ylabel("Salary")
plt.show()

"""## Visualising the Test set results"""

plt.scatter(X_test, y_test, color="red")
plt.plot(X_train, regressor.predict(X_train), color="blue")
plt.title("Salary vs Experiance (Test Set)")
plt.xlabel("Years of Experiance")
plt.ylabel("Salary")
plt.show()

# Comparing Actual Vs Predicted
df = pd.DataFrame({'Actual' : y_test, 'Predicted' : y_pred})
df

# Test Prediction with own data
exp = 10
pred_own = regressor.predict([[exp]])
print("Year of experiance ={}".format(exp))
print("Predicted Salary is = {}".format(pred_own[0]))